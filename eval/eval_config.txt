# System benchmark config for eval/run_models_bench.py
# Format: KEY=VALUE

# Model selection
MODEL_ID=Qwen/Qwen3.5-9B
# Optional override. If empty, derived from MODEL_ID.
# BASE_NAME=qwen3_5-9b

# Option A: resolve files from QUANTS -> ./models/<base>-<quant>.gguf
QUANTS=q8_0 q6_K q5_K_M q4_K_M q4_0

# Option B: explicit model files (comma-separated). If set, QUANTS is ignored.
# MODEL_FILES=./models/qwen3_5-9b-q8_0.gguf,./models/qwen3_5-9b-q4_K_M.gguf
MODEL_FILES=

# Paths
MODELS_DIR=./models
LLAMA_CPP_DIR=./llama.cpp

# Prompt source priority
# 1) PROMPT (single prompt) if non-empty
# 2) DATASET_PATH prompts (if dataset file has entries)
# 3) PROMPTS (split by ||)
PROMPT=
DATASET_PATH=./eval/eval_dataset.json
PROMPTS=Explain gradient descent in 3 concise bullet points.||What is overfitting and how do you mitigate it?||Compare precision and recall briefly.

# Generation hyperparameters
TEMP=0.1
N_PREDICT=128
CTX_SIZE=4096

# Runtime settings
THREADS=-1
N_GPU_LAYERS=auto
REPETITIONS=2
WARMUP_PROMPTS=1
SERVER_PORT_BASE=8088
SERVER_START_TIMEOUT_SEC=120
REQUEST_TIMEOUT_SEC=300
MEMORY_POLL_INTERVAL_SEC=0.10

# Outputs
OUTPUT_CSV=./eval/bench_results.csv
OUTPUT_JSON=./eval/bench_results.json
SERVER_LOG_DIR=./logs/eval
